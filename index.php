<?php

//retrieve cookie. 
  //if user does not have a valid session, display login screen
    /*
    request gcc email. send code to login.
    if the user does not exist yet in the database, add them.
    use email to validate user
    if user not validated within a certain time, delete
    create session cookie
    */
  //if user has a valid session continue.

?>
